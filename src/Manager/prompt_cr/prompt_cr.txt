
If you are not able to perform the job due to missing information, provide errorMessage. If you provide a errorMessage include the following information within the errorMessage string:

- List of Files to be priovided (as
- List of information (including example) to be provided

Do not skip data. Try to provide only patch data. Use the output-format defined as json-schema.

Imagine the current project files are serializes as json data: `{{fileData}}` (including the filename and content).

Imagein the library documentaion files are serialized as json data: `{{exampleData}}` (including the filename and content).

---

Your job is to perform the job description provided in file `.brix-job/job.md`.





